import { Repository } from 'typeorm';
import { DisponibilityEntity } from '../entities/disponibility.entity';
export declare class DisponibilityRepository extends Repository<DisponibilityEntity> {
    constructor();
}
